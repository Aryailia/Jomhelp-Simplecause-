<% organisation = arg %>
<div class="card">
  <div class="card-image"><img src="<%= organisation.photos %>"/></div>
  <div class="card-name"><strong><%= link_to(organisation.name, organisation) %></strong></div>
  <div class="card-content">
    <%= organisation.description -%>
  </div>
</div>