<div class = "container">
	<div class="jumbotron jumbotron-fluid">
	  <div class="container">
	    <h1 class="display-3">Fluid jumbotron</h1>
	    <p class="lead">This is a modified jumbotron that occupies the entire horizontal space of its parent.</p>
	  </div>
	</div>
	
	<h3>Past Events</h3>
	<div class = "row">
		<div class="card" style="width: 20rem;">
		  <img class="card-img-top" src="..." alt="Card image cap">
		  <div class="card-block">
		    <h4 class="card-title">Card title</h4>
		    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
		    <a href="#" class="btn btn-primary">Go somewhere</a>
		  </div>
		</div>
	</div>

	<h1><%= @organisation.name %></h1>
	<h1><%= @organisation.description %></h1>
	<h1><%= params[:notice] %></h1>
	<% if signed_in? %>
		<% if Organisation.find(params[:id]).contributors.find_by(user_id: current_user.id).nil? %>
			<%= button_to "Contribute", organisation_contributors_path(params[:id]), method: :post %>
		<% elsif Organisation.find(params[:id]).contributors.find_by(user_id: current_user.id).admin? %>
			<h1> You are Admin </h1>
		<% else %>
			<% @destroy_contributor = Contributor.where(organisation_id: params[:id]). find_by(user_id: current_user.id) %>
			<%= button_to "Contribute", uncontribute_path(@destroy_contributor), method: :delete, class: "btn btn-success" %>
		<% end %> 

		<% if contributor?(@organisation) %>
			<% if true || @contributor.member? -%>
				<div>
					<%= link_to('Create Event', new_organisation_event_path(@organisation)) %>
				</div>
			<% end -%>
			<%= link_to "Admin Dashboard", admin_dashboard_path(@organisation) %>
		<% end %>
	<% end %>

	<%= link_to 'All Organisation', organisations_path %>

	<% if signed_in? %>
		<% if !contributor?(@organisation) %>
			<% if !current_user.follow_organisations.exists?(@organisation.id) %>
				<%= button_to 'Follow', organisation_follow_path(@organisation.id) %>
			<% else %>
				<%= button_to 'Unfollow', organisation_unfollow_path(@organisation.id), method: :delete %>
			<% end %>
		<% end %>
	<% end %>


	<%= form_for @post do |f| %>
		<!-- post post_path -->
	 	<h4>Status:</h4>
	 	<%= f.text_area :content %><br>
	 	<%= f.hidden_field :organisation_id, value: @organisation.id %>
	 	<br>
		<br>
		<%= f.submit %></br>

		<% @organisation.posts.each do |o| %>
			<%= o.content %>

			<% if contributor?(@organisation) %>

				<%= link_to "Delete post", post_path(o), method: :delete , data: { confirm: 'Sure ah?'}  %>
				<%= link_to "Edit post", [:edit, o] %>
					<% end %>
				<br>	


			<% end %>
	<% end %>
	
</div>

